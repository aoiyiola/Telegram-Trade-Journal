[phases.setup]
nixPkgs = ['nodejs-18_x', 'python311']

[phases.build]
cmds = [
  'cd web && npm install && npm run build && cd ..',
  'pip install -r requirements.txt'
]

[start]
cmd = 'python start.py'
